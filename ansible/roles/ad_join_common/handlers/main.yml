---
- name: restart sssd
  ansible.builtin.systemd:
    name: sssd
    state: restarted

# Ubuntu names the unit ssh.service, Rocky/EL sshd.service.
- name: reload ssh
  ansible.builtin.systemd:
    name: "{{ 'ssh' if guest_os == 'ubuntu' else 'sshd' }}"
    state: reloaded
